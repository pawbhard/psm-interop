---
apiVersion: v1
kind: Service          
metadata:           
  name: ${service_name}
  namespace: ${namespace_name}
  labels:
    owner: xds-k8s-interop-test
spec:                   
  ports:      
  - name: grpc
    port: 8080
    protocol: TCP
    appProtocol: grpc
    targetPort: 8080
